<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.pc.infraredcontrol"
    tools:ignore="MissingLeanbackLauncher">

    <uses-permission android:name="android.permission.TRANSMIT_IR" />
    <!-- 声明调用红外设备的权限 -->
    <uses-feature android:name="android.hardware.ConsumerIrManager" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" /> <!-- 应用市场标签 -->
    <application
        android:name=".EventProcessing"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_study" />
        <activity android:name=".Main_Interface_Activity"/>
        <activity android:name=".power_Activity"/>
        <activity android:name=".power_study_activity">
    </activity>
    </application>

</manifest>